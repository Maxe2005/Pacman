# Pacman

Un projet de Pacman en C avec la librairie SDL

## Lancer le programme avec linux

Installer les dépendances :

```bash
sudo apt-get install $(grep -v '^#' prog/requirements.txt)
```

Lancer le programme :

```bash
./prog
```
